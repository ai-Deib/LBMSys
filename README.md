Change the value of String url, user, and password to your own database;

String url = "jdbc:mysql://localhost:3306/yourDatabase";
        String user = "yourUser";
        String password = "yourPassword";


Do this in every table initiation.
